//This page uses Inline Styles

const Header = () => {
  return (
    <div
      style={{
        color: "blue",
        background: "beige",
        padding: "5px",
        fontSize: "26px",
        fontWeight: "bold"
      }}
    >
      Personal Digital Assistants
    </div>
  );
};

export default Header;
